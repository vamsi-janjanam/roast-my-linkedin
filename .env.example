# Copy to .env and fill in. The .env file is gitignored and read only by the
# server-side proxy (server/index.js) — the key is never exposed to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Port the Express proxy listens on (defaults to 8787).
PORT=8787
